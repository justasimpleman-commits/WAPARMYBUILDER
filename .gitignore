# Dependencies (reinstalled by `npm install` / the build scripts)
node_modules/

# Build output (regenerated by electron-builder)
dist/
releases/

# Stale build snapshots (re-synced from root; see CLAUDE.md)
mac-installer-build/
windows-installer-build/

# Installers / packages
*.dmg
*.exe
*.blockmap
*.apk
*.aab

# Mobile (Capacitor) build artifacts
mobile/android/.gradle/
mobile/android/build/
mobile/android/app/build/
mobile/android/capacitor-cordova-android-plugins/
mobile/android/local.properties

# OS / editor clutter
.DS_Store
Thumbs.db
.idea/
.trash/

# Reference material — large third-party PDFs, not needed to run the app
reference/Army books/
