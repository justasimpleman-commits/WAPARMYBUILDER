# Dependencies (npm install — only needed for the changelog .docx script)
node_modules/

# Per-version source snapshots for changelog diffs (scripts/snapshot-version.js)
releases/

# OS / editor clutter
.DS_Store
Thumbs.db
.idea/
.trash/

# Reference material — large third-party PDFs, not needed to run the app
reference/Army books/
