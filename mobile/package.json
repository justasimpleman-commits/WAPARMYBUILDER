{
  "name": "warhammer-army-builder-mobile",
  "productName": "Warhammer Army Builder",
  "version": "1.4.0",
  "description": "Cross-platform mobile build (Capacitor) of the Warhammer 9th Ed 3.0 Army Builder. Wraps the same index.html + data files used by the desktop app.",
  "author": "András",
  "license": "MIT",
  "scripts": {
    "sync-web": "node sync-web.js",
    "build": "npm run sync-web && npx cap sync android",
    "open": "npx cap open android",
    "apk": "npm run build && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
